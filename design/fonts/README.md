# Brand webfonts (not committed)

`Druk-Web-Bold-Regular.woff2` and `supply-regular-webfont.woff2` are the licensed webfonts
served by the current site (wp-content/uploads). They are git-ignored. To regenerate the
mockups, download them from the live site into this folder first.
